// ============================================================
// tab-match.js — the Match tab (default landing tab)
// ============================================================

function MatchTab({ profile }) {
  return <Placeholder emoji="💘" title="配對" />;
}
